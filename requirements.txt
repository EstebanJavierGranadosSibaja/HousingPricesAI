# Core runtime dependencies (train, predict, app)
# Versiones fijadas para garantizar reproducibilidad y compatibilidad de los
# artefactos .joblib (pickle de scikit-learn). Probado con Python 3.11.
pandas==2.3.3
numpy==2.4.3
scikit-learn==1.8.0
joblib==1.5.3
streamlit==1.57.0

# Explainability layer (SHAP + LLM via Ollama)
shap>=0.46.0
python-dotenv>=1.0.0
requests>=2.31.0
